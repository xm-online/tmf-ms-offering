apply plugin: "war"

bootWar {
    mainClassName = "com.icthh.xm.tmf.ms.offering.OfferingApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
